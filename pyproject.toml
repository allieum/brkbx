[project]
name = "brkbx"
requires-python = ">=3.10"
dependencies = [
    "mpremote>=1.22",
    "pyserial>=3.3",
    "pyelftools>=0.31",
    "mpy-cross>=1.27.0.post2",
]

[project.optional-dependencies]
dev = ["pyright>=1.1"]
